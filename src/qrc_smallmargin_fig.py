"""PROVENANCE STUB — not part of the published B13 pipeline.

This filename was accidentally introduced by the 2026-07-04 scheduled session
(commit a2ed28d), which started from a stale CDN copy of the README (pre-B13),
re-implemented the small-margin sweep, and pushed its own figure script here —
alongside an overwrite of `src/qrc_smallmargin.py` — before discovering that B13
was already published. The published B13 code was restored in the commit that
added this stub, and the replication session's full figure code remains
available in git history at commit a2ed28d.

The published B13 figure is `figures/qrc_smallmargin.png`, produced by the
2026-07-03 run; the published raw cells are `results/smallmargin_law.json`
(file of record, untouched). See AUDITS.md, entry "2026-07-03 — B13", second
addendum, for the full incident log and the replication's side-by-side numbers.
"""
